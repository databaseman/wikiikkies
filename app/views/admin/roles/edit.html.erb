<header>
  <h1>Edit Role</h1>
</header>
<%= simple_form_for([:admin, @role]) do |f| %>
  <%= f.input :name, disabled: true %>
  <%= f.input :description %>

  <%= f.button :submit, class: "btn-primary" %>
<% end %>
